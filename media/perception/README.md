# Perception media

| File | What it is | Used by |
|---|---|---|
| `scan-overlay.jpg` | Shot 11 of the 12-shot centre scan with A1 boxes, face identities, range and grid cell drawn on, plus an enlarged panel where the labels are legible | `README.md`, `perception/README.md` |
| `scan.mp4` | The full twelve-shot scan, one second per shot, captioned with yaw and detection count | `perception/README.md` |

Both come off the robot, not the simulator: Final 1
(`20260724_140013_e2e`), the scan that produced the most detections of the four
matches (10 detections, 10 voted, on the shot used for the still).

The frames are cropped below the arena wall. The raw captures look past it into
the venue, where spectators are identifiable; the crop is deliberate and was
checked against all twelve shots, not just the published one.
